Cel: Utwórz szkielet klikalnego prototypu modułu Krokomierz.

Instrukcje:
- Stwórz projekt React + Vite + Tailwind.
- Strony: `/map`, `/pedometer/:deviceId`, `/history/:deviceId`.
- Komponenty: `StepsWidget`, `PedometerCircle`, `GoalModal`, `DayDetailModal`, `HistoryChart`.
- Mock danych: `src/mocks/pedometer.json` (dziecko/senior/zwierzę, różne cele i kroki).
- Na `/map` umieść `StepsWidget` (70×70, top-center), klik → `/pedometer/:deviceId`.
- Routing w `src/App.jsx`, dane mockowane lokalnie.

Output:
- Komplet plików projektu.
- Krótkie README z instrukcją uruchomienia.
Źródła: patrz `.cursorrules` + `docs/`.